import '@testing-library/jest-dom';
import {render, screen} from '@testing-library/react';

describe('Testing header with context', ()=>
{
  test('Should render the count of incomplete items', ()=>
  {
    // render app
    // add todo list item
    // find nav.header with test class name
    // expect it to be rendered on screen with '1 item pending'
  });
})
