import '@testing-library/jest-dom';
import {render, screen} from '@testing-library/react';

describe('Testing lists with context', ()=>
{
  test('Should render a list items with context', ()=>
  {
    // render app
    // 
  });
})
